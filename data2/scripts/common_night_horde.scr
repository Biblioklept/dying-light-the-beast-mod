sub main()
{
    NightHorde()
    {
		DefaultRadius(10.0);
		MaxRadius(500.0);		
		TimeToReachMaxRadius(10.0);
		TimeOutsideHordeToDisable(30.0);
		DistToDisableNightHorde(500.0);
		
		PlayerDistanceToChangeSpawnPos(10.0);
		
		VolatilesChaseMaxDistance(5.0); //5.0
		VolatilesChaseMaxNumber(2); //1.0
		
		FogBlendDistance(5.0);
		
		BehaviourWander()
		{
			// Maximum distance the horde might move away from its base position.
			Range()
			{
				Value(0, 20.0);
				Value(1, 70.0);
				Value(2, 100.0);
			}
			
			// Determines how fast the horde moves when wandering.
			Speed()
			{
				Value(0, 1.0);
				Value(1, 2.0);				
				Value(2, 3.0);
			}
		}
		
		BehaviourChase()
		{
			MaxSpeed(3.75);
			MinSpeed(3.0, 15.0); //  first is value of speed, second is distance that this speed is reached		//2 15	
		}
    }
}
